#pragma once
//enumerados
enum diaSemana { SEG, TER, QUA, QUI, SEX, SAB, DOM };
typedef enum diaSemana diaSemana;

typedef enum clubes { BENFICA, PORTO, SPORTING, BRAGUINHA } clubes;

enum cores {RED, YELLOW, GREEN};




